\"\"\" \n================================================================\nAYESHA AI\nDIALOGUE MODULE - FIXED VERSION\n\nVersion : 1.1 (Optimized)\nStatus  : Production\n\nPurpose\n-------\nConversation Manager with O(1) search\n\nFeatures\n--------\n✓ Chat History\n✓ Session Management\n✓ O(1) Last Message Lookup\n✓ Context Memory\n✓ Response Queue\n✓ Bangla + English\n✓ Thread Safe\n✓ Future LLM Ready\n✓ Future TTS Ready\n✓ EventBus Ready\n\n================================================================\n\"\"\"\n\nfrom __future__ import annotations\n\nimport logging\nimport uuid\nimport threading\nfrom collections import deque\nfrom dataclasses import dataclass, field\nfrom datetime import datetime\nfrom typing import Deque, Dict, List, Optional\n\nlogger = logging.getLogger(\"AYESHA_CORE\")\n\n# ==========================================================\n# MESSAGE\n# ==========================================================\n\n@dataclass(slots=True)\nclass DialogueMessage:\n    role: str\n    content: str\n    timestamp: datetime = field(default_factory=datetime.now)\n\n# ==========================================================\n# SESSION\n# ==========================================================\n\nclass DialogueSession:\n    \"\"\"Dialogue session with optimized lookups\"\"\"\n    \n    def __init__(self):\n        self.session_id = str(uuid.uuid4())\n        self.created_at = datetime.now()\n        self.messages: List[DialogueMessage] = []\n        \n        # ✅ FIX #1: Pointers for O(1) lookup\n        self.last_user_index = -1\n        self.last_assistant_index = -1\n        \n        logger.info(\"Dialogue Session Created\")\n\n# ==========================================================\n# CONVERSATION MANAGER - FIXED VERSION\n# ==========================================================\n\nclass ConversationManager:\n    \"\"\"\n    Optimized Conversation Manager with:\n    - O(1) last message lookup (instead of O(n))\n    - Thread-safe operations\n    - Session archive\n    - Proper error handling\n    \"\"\"\n\n    def __init__(self):\n        self.session = DialogueSession()\n        \n        # ✅ FIX #2: Session archive\n        self.sessions_history: List[DialogueSession] = []\n        \n        self.response_queue: Deque[str] = deque()\n        self.context: Dict[str, str] = {}\n        \n        # ✅ Thread safety\n        self.lock = threading.RLock()\n        \n        logger.info(\"Conversation Manager Initialized (Optimized)\")\n\n    # ------------------------------------------------------\n    # Add Message\n    # ------------------------------------------------------\n\n    def add_message(self, role: str, content: str) -> None:\n        \"\"\"Add message with O(1) index update\"\"\"\n        with self.lock:\n            message = DialogueMessage(role=role, content=content)\n            self.session.messages.append(message)\n            \n            # ✅ Update pointers for O(1) lookup\n            if role == \"user\":\n                self.session.last_user_index = len(self.session.messages) - 1\n                logger.debug(f\"User message added at index {self.session.last_user_index}\")\n            \n            elif role == \"assistant\":\n                self.session.last_assistant_index = len(self.session.messages) - 1\n                logger.debug(f\"Assistant message added at index {self.session.last_assistant_index}\")\n            \n            else:\n                logger.debug(f\"{role} message added\")\n\n    # ------------------------------------------------------\n    # History\n    # ------------------------------------------------------\n\n    def get_history(self) -> List[DialogueMessage]:\n        \"\"\"Get conversation history\"\"\"\n        with self.lock:\n            return list(self.session.messages)\n\n    # ------------------------------------------------------\n    # Clear History\n    # ------------------------------------------------------\n\n    def clear_history(self) -> None:\n        \"\"\"Clear conversation history\"\"\"\n        with self.lock:\n            self.session.messages.clear()\n            self.session.last_user_index = -1\n            self.session.last_assistant_index = -1\n            logger.info(\"Conversation History Cleared\")\n\n    # ------------------------------------------------------\n    # Count\n    # ------------------------------------------------------\n\n    def message_count(self) -> int:\n        \"\"\"Get total message count\"\"\"\n        with self.lock:\n            return len(self.session.messages)\n\n    # ------------------------------------------------------\n    # Context\n    # ------------------------------------------------------\n\n    def set_context(self, key: str, value: str) -> None:\n        \"\"\"Set context variable\"\"\"\n        with self.lock:\n            self.context[key] = value\n            logger.info(f\"Context Updated: {key}\")\n\n    def get_context(self, key: str, default: Optional[str] = None) -> Optional[str]:\n        \"\"\"Get context variable\"\"\"\n        with self.lock:\n            return self.context.get(key, default)\n\n    def clear_context(self) -> None:\n        \"\"\"Clear all context\"\"\"\n        with self.lock:\n            self.context.clear()\n            logger.info(\"Context Cleared\")\n\n    # ------------------------------------------------------\n    # Response Queue\n    # ------------------------------------------------------\n\n    def queue_response(self, response: str) -> None:\n        \"\"\"Queue response for sending\"\"\"\n        with self.lock:\n            self.response_queue.append(response)\n\n    def next_response(self) -> Optional[str]:\n        \"\"\"Get next queued response\"\"\"\n        with self.lock:\n            if not self.response_queue:\n                return None\n            return self.response_queue.popleft()\n\n    def queue_size(self) -> int:\n        \"\"\"Get queue size\"\"\"\n        with self.lock:\n            return len(self.response_queue)\n\n    # ✅ FIX #1: O(1) Last Messages\n    # =====================================================\n\n    def last_message(self) -> Optional[DialogueMessage]:\n        \"\"\"Get last message (O(1))\"\"\"\n        with self.lock:\n            if not self.session.messages:\n                return None\n            return self.session.messages[-1]\n\n    def last_user_message(self) -> Optional[DialogueMessage]:\n        \"\"\"Get last user message - O(1) instead of O(n)\"\"\"\n        with self.lock:\n            if self.session.last_user_index >= 0:\n                return self.session.messages[self.session.last_user_index]\n            return None\n\n    def last_assistant_message(self) -> Optional[DialogueMessage]:\n        \"\"\"Get last assistant message - O(1) instead of O(n)\"\"\"\n        with self.lock:\n            if self.session.last_assistant_index >= 0:\n                return self.session.messages[self.session.last_assistant_index]\n            return None\n\n    # =====================================================\n\n    # ✅ FIX #2: Session Archive\n    # =====================================================\n\n    def remove_last_message(self) -> Optional[DialogueMessage]:\n        \"\"\"Remove last message\"\"\"\n        with self.lock:\n            if not self.session.messages:\n                return None\n            \n            message = self.session.messages.pop()\n            \n            # Update pointers\n            if len(self.session.messages) > 0:\n                if message.role == \"user\" and self.session.last_user_index == len(self.session.messages):\n                    self.session.last_user_index -= 1\n                elif message.role == \"assistant\" and self.session.last_assistant_index == len(self.session.messages):\n                    self.session.last_assistant_index -= 1\n            else:\n                self.session.last_user_index = -1\n                self.session.last_assistant_index = -1\n            \n            logger.info(\"Last Message Removed\")\n            return message\n\n    # ==============================================\n\n    # Session Information\n    # ==============================================\n\n    def session_id(self) -> str:\n        \"\"\"Get current session ID\"\"\"\n        with self.lock:\n            return self.session.session_id\n\n    def created_time(self) -> datetime:\n        \"\"\"Get session creation time\"\"\"\n        with self.lock:\n            return self.session.created_at\n\n    # ==============================================\n    # Export Conversation\n    # ==============================================\n\n    def export_history(self) -> List[dict]:\n        \"\"\"Export conversation history\"\"\"\n        history = []\n        with self.lock:\n            for message in self.session.messages:\n                history.append({\n                    \"role\": message.role,\n                    \"content\": message.content,\n                    \"timestamp\": message.timestamp.isoformat()\n                })\n        return history\n\n    # ==============================================\n    # ✅ FIX #2: Reset with Archive\n    # ==============================================\n\n    def reset_session(self) -> None:\n        \"\"\"Reset session (archive old one)\"\"\"\n        with self.lock:\n            # Archive current session\n            self.sessions_history.append(self.session)\n            logger.info(f\"Session archived: {self.session.session_id}\")\n            \n            # Create new session\n            self.session = DialogueSession()\n            self.response_queue.clear()\n            self.context.clear()\n            \n            logger.info(\"Dialogue Session Reset\")\n\n    def get_session_history(self, limit: int = 10) -> List[DialogueSession]:\n        \"\"\"Get archived sessions\"\"\"\n        with self.lock:\n            return self.sessions_history[-limit:]\n\n    # ==============================================\n\n    # Status\n    # ==============================================\n\n    def is_empty(self) -> bool:\n        \"\"\"Check if conversation is empty\"\"\"\n        with self.lock:\n            return len(self.session.messages) == 0\n\n    # ==============================================\n    # Recent Messages\n    # ==============================================\n\n    def recent_messages(self, limit: int = 5) -> List[DialogueMessage]:\n        \"\"\"Get recent messages\"\"\"\n        with self.lock:\n            if limit <= 0:\n                return []\n            return self.session.messages[-limit:]\n\n    # ==============================================\n    # Statistics\n    # ==============================================\n\n    def statistics(self) -> dict:\n        \"\"\"Get conversation statistics\"\"\"\n        with self.lock:\n            return {\n                \"session_id\": self.session.session_id,\n                \"messages\": len(self.session.messages),\n                \"user_messages\": sum(\n                    1 for m in self.session.messages if m.role == \"user\"\n                ),\n                \"assistant_messages\": sum(\n                    1 for m in self.session.messages if m.role == \"assistant\"\n                ),\n                \"context_items\": len(self.context),\n                \"queue_size\": len(self.response_queue),\n                \"created\": self.session.created_at.isoformat(),\n                \"archived_sessions\": len(self.sessions_history),\n                \"version\": \"1.1 (Optimized)\"\n            }\n\n    # ==============================================\n    # Magic Methods\n    # ==============================================\n\n    def __len__(self) -> int:\n        \"\"\"Length of conversation\"\"\"\n        with self.lock:\n            return len(self.session.messages)\n\n    def __iter__(self):\n        \"\"\"Iterate over messages\"\"\"\n        with self.lock:\n            return iter(list(self.session.messages))\n\n# ==========================================================\n# GLOBAL DIALOGUE INSTANCE\n# ==========================================================\n\ndialogue = ConversationManager()\n